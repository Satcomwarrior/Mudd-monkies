# Path to the ConstructConnect/Claude artifact bundle used by the proprietary LLM
CLAUDE_AI_ARTIFACTS_PATH="/absolute/path/to/CLAUDE_AI_ARTIFACTS"

# Endpoint that executes the ConstructConnect guidance model (CLI wrapper, REST gateway, etc.)
CONSTRUCTCONNECT_MODEL_ENDPOINT="https://your-ai-gateway.example.com/guidance"

# Optional API key forwarded to the ConstructConnect model endpoint
CONSTRUCTCONNECT_MODEL_API_KEY="your-api-key"
